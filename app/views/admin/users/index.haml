%p There are currently #{@user_list.count} users

%p This is a full list of users currently signed up to RAOC

%table.table.table-striped
  %tr
    %th First name
    %th Last name
    %th Email
    %th Action
  - @user_list.each do |user_object|
    %tr
      %td #{user_object.first_name}
      %td #{user_object.last_name}
      %td #{user_object.email}
      %td
        = link_to("edit", edit_admin_user_path(user_object))
        &bull;
        = link_to("delete", admin_user_path(user_object), method: :delete, data: { confirm: "Are you absolutely sure you want to permanently remove #{user_object.full_name}?" })
